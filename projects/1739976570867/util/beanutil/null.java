/**
* users对象Get Set
* @author capkin 2025-02-19
*/

// id
Long id = users.getId();

// id
users.setId();

// id
users.setId(users2.getId());

// id
map.put("id",users.getId());

// id
map.put("id",users.getId());

map.put("id",users.getId());

// id
map.put("id",users.getId());

